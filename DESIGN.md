# DESIGN.md

mymind のデザインシステムです。画面を作るとき、直すときは、この文書のトークンとコンポーネントを使ってください。ここにない値を新しく使う場合は、先にこの文書へ追加してから実装します。

## 0. Figma（画面の正本）

画面の構成は Figma の **mymind view design** が正本です。この文書はトークンとルールの正本で、両者は同じ値でなければなりません。

- ファイル：https://www.figma.com/design/VLCoEFLm1ujvYPq8xyEQFg/mymind-view-design （fileKey `VLCoEFLm1ujvYPq8xyEQFg`）
- `PC` ページ：`PC/今日`、`PC/朝の計画`、`PC/振り返り`、`PC/バックログ`、`PC/タイムライン`、`PC/カレンダー`（各 1440×900）
- `Components` ページ：`Mame`、`StatusIcon`、`Chip`、`Button`、`Kbd`、`GlassPanel`、色の変数（コレクション `mymind`）、テキストスタイル、エフェクトスタイル

どのフレームを見ればよいか、node-id の取り方、Figma と CSS の対応、既知の差分は `docs/design/README.md` にまとめています。

## 1. デザイン原則

**すりガラスの静けさ。** やわらかい色のにじみを置いた背景の上に、半透明のガラスの面を重ねます。色は背景のにじみに任せ、面そのものは白の透過で表現します。

**アクセントは藍色1色。** 操作を促す要素（主ボタン、選択状態、進行中の状態）だけに藍色を使います。それ以外の色は状態色とマメの色に限ります。

**スマートな文字組み。** 書体は欧文Inter、和文Noto Sans JPの組み合わせに統一します。見出しは太くしすぎず、字間を詰めて引き締めます。

**事実は控えめに、考えたことは読みやすく。** 件数や日数などの事実は小さく等幅数字で添え、振り返りやFBの文章は行間を広く取って読みやすくします。

**キーボードで完結する。** 操作できる要素には、可能な限りキーボードショートカットを割り当て、画面上にも `kbd` で示します。

## 2. デザイントークン

実装ではCSS変数として定義します。変数名は `--` から始まるこの表の名前を使います。

### 2.1 色

| トークン           | 値                        | 用途                                                 |
| ------------------ | ------------------------- | ---------------------------------------------------- |
| `--ink-1`          | `#1C1B19`                 | 本文、見出し                                         |
| `--ink-2`          | `#3F3B34`                 | 補助的な本文、ナビゲーション                         |
| `--ink-3`          | `#4F4A42`                 | セクションラベル、説明文                             |
| `--ink-4`          | `#5A554C`                 | キャプション（これより薄い色を小さい文字に使わない） |
| `--ground`         | `#F1ECE3`                 | 背景の地色                                           |
| `--ground-evening` | `#E9E6EE`                 | 振り返り画面の地色                                   |
| `--accent`         | `#2F4B7C`                 | 主ボタン、選択状態、着手中                           |
| `--accent-strong`  | `#233A63`                 | 薄い藍色の上の文字                                   |
| `--accent-soft`    | `rgba(47, 75, 124, 0.12)` | 選択中のナビゲーション、FBの「一手」の背景           |

背景のにじみは、次の4色を円形のグラデーションとして画面の角や端に置きます。1画面に3〜4個まで、中心付近には置きません。

| トークン         | 値                          |
| ---------------- | --------------------------- |
| `--blob-indigo`  | `rgba(122, 150, 214, 0.55)` |
| `--blob-apricot` | `rgba(244, 176, 124, 0.50)` |
| `--blob-sage`    | `rgba(138, 192, 146, 0.42)` |
| `--blob-lilac`   | `rgba(196, 164, 222, 0.45)` |

### 2.2 状態色

| 状態   | アイコンの色（`--status-*`） | バッジの背景（`--status-*-bg`） | バッジの文字（`--status-*-text`） | タイムラインの表現                                                             |
| ------ | ---------------------------- | ------------------------------- | --------------------------------- | ------------------------------------------------------------------------------ |
| 未着手 | `#8A857B`                    | `rgba(120, 112, 98, 0.14)`      | `#4A463F`                         | 表示しない                                                                     |
| 着手中 | `#2F4B7C`                    | `rgba(47, 75, 124, 0.14)`       | `#233A63`                         | 塗りつぶし                                                                     |
| 中断   | `#8A6D3B`                    | `rgba(138, 109, 59, 0.16)`      | `#6E5528`                         | 淡い塗り（`#EADFC6`）＋破線の枠（`#8A6D3B`、4px と 3px の繰り返し、太さ1.2px） |
| 待ち   | `#9A4F32`                    | `rgba(154, 79, 50, 0.14)`       | `#7E3F26`                         | 塗り（`rgba(154, 79, 50, 0.5)`）                                               |
| 完了   | `#3F6B4E`                    | `rgba(63, 107, 78, 0.14)`       | `#2C4F38`                         | 塗りつぶし（完了したタスクの期間）                                             |
| 中止   | `#8A857B`                    | `rgba(120, 112, 98, 0.14)`      | `#4A463F`                         | 表示しない                                                                     |

トークンの `*` には、ステータスの識別子（`todo`、`doing`、`paused`、`waiting`、`done`、`cancelled`）が入ります（例：`--status-doing-bg`）。

状態は色だけで区別せず、アイコンの形（空の円、半分塗り、時計、一時停止、チェック）とラベルを必ず併用します。

### 2.3 ガラス

| トークン    | 背景                        | ぼかし                      | 用途                         |
| ----------- | --------------------------- | --------------------------- | ---------------------------- |
| `--glass-1` | `rgba(255, 255, 255, 0.50)` | `blur(24px) saturate(170%)` | サイドバー                   |
| `--glass-2` | `rgba(255, 255, 255, 0.55)` | `blur(22px) saturate(170%)` | カード、リストの面           |
| `--glass-3` | `rgba(255, 255, 255, 0.60)` | `blur(26px) saturate(180%)` | 詳細ペイン、入力欄           |
| `--glass-4` | `rgba(255, 255, 255, 0.78)` | `blur(30px) saturate(180%)` | コマンドパレット、ダイアログ |

ガラスの面には共通して、白の縁取り `1px solid rgba(255, 255, 255, 0.85)`、上端のハイライト `inset 0 1px 0 rgba(255, 255, 255, 0.95)`、影 `0 12px 32px rgba(38, 34, 28, 0.10)` を付けます。

`backdrop-filter` は描画負荷が高いため、リストの各行には使いません。行を包む外側の面にだけ使います。

CSS 変数では、背景を `--glass-1`〜`--glass-4`、ぼかしを `--glass-1-blur`〜`--glass-4-blur`、共通の縁取りを `--glass-border`、上端のハイライトと影をまとめて `--glass-shadow`、不透明な面を `--glass-opaque` と `--glass-opaque-border` とします。

OS の「透明度を下げる」設定（`prefers-reduced-transparency: reduce`）が有効な場合は、ガラスの面を不透明な面（`#FBF9F5`、枠線 `1px solid rgba(28, 27, 25, 0.10)`）に切り替え、背景の色のにじみも消します。「視差効果を減らす」設定（`prefers-reduced-motion: reduce`）では、画面の切り替えやマメの表情の変化のアニメーションを止めます（NFR-22）。

### 2.4 角の丸み

| トークン        | 値      | 用途                                                   |
| --------------- | ------- | ------------------------------------------------------ |
| `--radius-s`    | `6px`   | `kbd`                                                  |
| `--radius-m`    | `12px`  | ナビゲーション項目、小さなボタン、セグメントの中の項目 |
| `--radius-l`    | `14px`  | リストの行、セグメントコントロールの外枠               |
| `--radius-xl`   | `22px`  | カード、入力欄、主ボタン                               |
| `--radius-2xl`  | `24px`  | ペイン                                                 |
| `--radius-full` | `999px` | バッジ、ピル                                           |

### 2.5 余白

4pxを基準にした `4, 8, 12, 16, 20, 24, 32` だけを使います。CSS 変数は、順に `--space-1`、`--space-2`、`--space-3`、`--space-4`、`--space-5`、`--space-6`、`--space-8`（数字は 4px の何倍か）です。3ペインの外周の余白とペイン同士の間隔は `16px`、ペインの内側は `24px` を基本にします。

### 2.6 書体

```css
font-family: "Inter", "Noto Sans JP", "Hiragino Sans", sans-serif;
font-feature-settings: "cv11", "ss01";
```

書体の指定は CSS 変数 `--font-sans`（上の `font-family`）と `--font-mono`（コード）で使います。

| 役割    | サイズ / 太さ / 字間       | 用途                               |
| ------- | -------------------------- | ---------------------------------- |
| Display | 30px / 600 / -0.025em      | 画面の見出し（日付、画面名）       |
| Title   | 22px / 600 / -0.02em       | 詳細ペインの見出し                 |
| Heading | 15px / 600 / -0.01em       | セクションの見出し、入力欄のラベル |
| Label   | 12px / 600 / 0.06em        | セクションラベル                   |
| Body    | 14px / 400〜500 / 行間 1.7 | 本文、タスク名                     |
| Small   | 12〜13px / 400             | 補足                               |
| Caption | 11px / 400〜600            | バッジ、日付の注記                 |
| Code    | 12.5px、`ui-monospace`     | Markdownのコード                   |

件数、日数、時刻などの数字には `font-variant-numeric: tabular-nums` を指定し、桁を揃えます。

フォントはアプリに同梱して自前で配信します（NFR-10）。Inter と Noto Sans JP は `@fontsource` の WOFF2（文字の範囲ごとに分かれたもの）を、400・500・600・700 の4つの太さだけ同梱します。ブラウザは `unicode-range` で、表示する文字を含むファイルだけを読み込みます（ADR-0012）。外部のフォント配信サービスは使いません。

## 3. レイアウト

PCは3ペイン構成です。

| ペイン     | 幅              | 内容                                                                                                                         |
| ---------- | --------------- | ---------------------------------------------------------------------------------------------------------------------------- |
| サイドバー | 232px 固定      | アプリ名、ナビゲーション（毎日：今日・朝の計画・振り返り／一覧：バックログ・タイムライン・カレンダー）、ショートカットの案内 |
| メイン     | 残り全部        | その画面の主な作業                                                                                                           |
| 詳細       | 380〜400px 固定 | 選択中の項目の詳細、FB、棚卸し                                                                                               |

基準の画面サイズは 1440×900 です。1280px 未満では詳細ペインを右からのオーバーレイに切り替えます。

## 4. コンポーネント

### 4.1 マメ（Mame）

調子を表すキャラクターです。必ず共通コンポーネント `Mame` を使い、画面ごとに描き直しません。

```tsx
<Mame mood="good" size={44} />
```

| mood     | 意味   | 体の色    | 特徴                          |
| -------- | ------ | --------- | ----------------------------- |
| `best`   | 絶好調 | `#F08C78` | 目を細めた笑顔、芽に花が咲く  |
| `good`   | 好調   | `#F2B27E` | 点の目と笑顔、葉が2枚         |
| `normal` | 普通   | `#E3CF86` | 点の目とまっすぐな口、葉が1枚 |
| `bad`    | 不調   | `#94B6DB` | 下がった目、芽が傾く          |
| `worst`  | 絶不調 | `#AE9FD6` | 閉じた横線の目、芽がしおれる  |
| `sleep`  | FBなし | `#D9D4CB` | 閉じた目                      |
| `think`  | 生成中 | `#E3CF86` | 上を見る目、考え中の泡        |

色はトークンで定義します。体の色は `--mame-<mood>`、その mood の影と泡の色は `--mame-<mood>-ink` です（`think` は `normal` と同じ色を使う）。

| トークン             | 値        | 用途                         |
| -------------------- | --------- | ---------------------------- |
| `--mame-best-ink`    | `#B4432F` | `best` の下側の影、考え中の泡 |
| `--mame-good-ink`    | `#B0621F` | `good` の同上                 |
| `--mame-normal-ink`  | `#8A7422` | `normal` と `think` の同上    |
| `--mame-bad-ink`     | `#35628F` | `bad` の同上                  |
| `--mame-worst-ink`   | `#5B4A8A` | `worst` の同上                |
| `--mame-sleep-ink`   | `#6B665C` | `sleep` の同上                |
| `--mame-face`        | `#1F1D1A` | 目と口の線                   |
| `--mame-stem`        | `#3E7A55` | 芽の茎、葉の縁               |
| `--mame-leaf`        | `#86BF98` | 葉の塗り                     |
| `--mame-flower`      | `#F4CF62` | `best` の花                   |
| `--mame-highlight`   | `#FFFFFF` | 縁取り、ハイライト、光の点   |

体は半透明の塗りに、白の縁取り、左上の弧のハイライト、小さな光の点、下側の薄い影を重ねたガラス玉の質感です。顔の線は `#1F1D1A`、太さは目1.9、口1.5（48×48の座標系）とします。サイズは、カレンダーとタイムラインで28〜44px、詳細ペインで56〜76px、空の状態の案内で96pxを目安にします。

### 4.2 ステータスアイコン

直径20〜24pxの円で状態を表し、クリックで次の状態へ進めます。クリック領域は40×40px以上にします。アイコンには「タスク名：状態（クリックで次の状態へ）」の `aria-label` を付けます。形は、未着手＝空の円、着手中＝半分塗り、待ち＝時計、中断＝一時停止、完了＝塗りつぶしの円にチェック、中止＝斜線の入った円です。色は2.2節の `--status-*` です。

選択中のタスクの行は、白の半透明（`rgba(255, 255, 255, 0.5)`、`--row-selected-bg`）の塗りと、藍色の枠（`1.5px solid rgba(47, 75, 124, 0.5)`、`--row-selected-border`）で示します。

### 4.3 バッジ

`状態・N日目` の形式で、状態色のバッジを使います。未着手と完了のタスクにはバッジを付けません。

### 4.4 ボタン

| 種類           | 見た目                                  | 用途                             |
| -------------- | --------------------------------------- | -------------------------------- |
| 主ボタン       | `--accent` の塗り、白文字               | 1画面に1つ。その画面の主な操作   |
| 確定ボタン     | `rgba(28, 27, 25, 0.88)` の塗り、白文字 | 計画の確定、保存してFBをもらう   |
| 副ボタン       | 白の半透明、`--ink-1` の文字            | 保存のみ、残す、キャンセル       |
| テキストボタン | 下線付きの `--accent` の文字            | もう一度もらう、など控えめな操作 |

ショートカットがある操作は、ボタンの中に `kbd` を添えます。

| トークン                    | 値                                 | 用途                               |
| --------------------------- | ---------------------------------- | ---------------------------------- |
| `--text-on-accent`          | `#FFFFFF`                          | 主ボタン・確定ボタンの文字         |
| `--accent-shadow`           | `0 8px 20px rgba(47, 75, 124, 0.28)` | 主ボタンの影                       |
| `--button-secondary-bg`     | `rgba(255, 255, 255, 0.4)`         | 副ボタンの塗り                     |
| `--button-secondary-border` | `1px solid rgba(255, 255, 255, 0.95)` | 副ボタンの枠                     |

### 4.5 kbd

白の半透明（`rgba(255, 255, 255, 0.8)`、`--kbd-bg`）、`1px solid rgba(28, 27, 25, 0.12)` の枠（`--kbd-border`）、11px、角の丸み `--radius-s` で表示します。濃い色のボタンの中では、背景を `rgba(255, 255, 255, 0.18)`（`--kbd-bg-on-dark`）、文字を白（`--text-on-accent`）にします。

### 4.6 FBの表示

FBは「よかったこと（緑の見出し）」「気づき（赤茶の見出し）」「明日の一手（藍色の薄い背景で囲む）」の順に並べ、その上にマメと調子、判定の根拠を置きます。末尾に生成日時と使ったエージェントを小さく表示します。

生成の状態は4つで、それぞれ次のように表示します。

| 状態   | 表示                                               |
| ------ | -------------------------------------------------- |
| 未依頼 | `sleep` のマメ、「まだFBはありません」、依頼ボタン |
| 生成中 | `think` のマメ、3段階の進捗、キャンセルボタン      |
| 失敗   | 赤茶の薄い背景に理由の要約、再試行ボタン           |
| 完了   | FB本文と「もう一度もらう」                         |

### 4.7 Markdownエディタとプレビュー

入力欄はガラスの面の中にCodeMirror 6を置き、下端に使える書き方の案内を表示します。プレビューでは、見出しをHeading、リストの記号を `--accent`、引用を `--accent-soft` の背景、コードを `rgba(28, 27, 25, 0.06)` の背景と等幅書体で表示します。引用に左側の太い線は使いません。

### 4.8 タイムラインの横棒

**横棒**は高さ18px、角の丸み9pxで、状態ごとに2.2節の表現を使います。中断と待ちは、色だけでなく形でも区別します（中断は破線の枠、待ちは塗り）。隣り合う日は横棒をつなげて描き、期間の先頭と末尾だけ角を丸めます。

**列と行**は次のようにします。

| 対象             | 指定                                                                         |
| ---------------- | ---------------------------------------------------------------------------- |
| 1日の列の幅      | 42px                                                                         |
| タスク名の列の幅 | 164px（左の余白12px、右の余白14px、はみ出す名前は末尾を省略）                |
| 行の高さ         | 日付の行52px、調子の行48px、タスクの行46px。行の間隔は4px                    |
| 表の内側の余白   | 上下左右18px（上だけ16px）                                                   |
| 今日の列         | 日付の行は `rgba(47, 75, 124, 0.14)`、タスクの行は `rgba(47, 75, 124, 0.07)` |
| 土日の列         | 日付の行は `rgba(28, 27, 25, 0.04)`、タスクの行は `rgba(28, 27, 25, 0.03)`   |
| 行の縞模様       | 1行おきに白14%。選択中の行はガラスの面（白50%）＋藍色の枠                    |
| 調子のレーン     | 背景を白26%にして、タスクの行と区切る                                        |

凡例と、詳細ペインの「期間の内訳」の積み上げ棒にも、同じ描き分けを使います。

## 5. キーマップ

キー割り当ては1か所の定義にまとめ、コマンドパレットとヘルプ（`?`）はこの定義から生成します。入力欄にフォーカスがあるときは、修飾キーなしのショートカットを無効にします。

### 5.1 どの画面でも使えるキー

| キー      | 操作                         |
| --------- | ---------------------------- |
| `⌘K`      | コマンドパレット             |
| `?`       | ショートカットの一覧         |
| `N`       | タスクを追加（入力欄へ移動） |
| `G` → `T` | 今日                         |
| `G` → `M` | 朝の計画                     |
| `G` → `R` | 振り返り                     |
| `G` → `B` | バックログ                   |
| `G` → `L` | タイムライン                 |
| `G` → `C` | カレンダー                   |
| `Esc`     | 選択の解除、パネルを閉じる   |

### 5.2 タスクのリスト

| キー              | 操作                                         |
| ----------------- | -------------------------------------------- |
| `J` / `↓`         | 次のタスク                                   |
| `K` / `↑`         | 前のタスク                                   |
| `Space` / `Enter` | 状態を進める                                 |
| `P`               | 中断                                         |
| `W`               | 待ち                                         |
| `X`               | 中止                                         |
| `T`               | 今日のリストでは明日へ、バックログでは今日へ |
| `B`               | バックログへ                                 |
| `E`               | タイトルを編集                               |
| `Tab` / `⇧Tab`    | 子タスクにする / 親に戻す                    |
| `⌘↑` / `⌘↓`       | 並べ替え                                     |

### 5.3 画面ごとのキー

| 画面     | キー            | 操作                                                                                            |
| -------- | --------------- | ----------------------------------------------------------------------------------------------- |
| 朝の計画 | `1` / `2` / `3` | 選択中の持ち越しを「今日もやる / バックログへ / 実は終わった」                                  |
| 朝の計画 | `⌘↵`            | 計画を確定                                                                                      |
| 振り返り | `⌘↵`            | 保存してFBをもらう                                                                              |
| 振り返り | `⌘S`            | 保存のみ                                                                                        |
| 振り返り | `⌘P`            | 書く / プレビューの切り替え（ブラウザの印刷と重なるため、アプリ内では印刷を無効にする。要検証） |
| 振り返り | `⌘B`            | 太字                                                                                            |
| 棚卸し   | `1` / `2` / `3` | 今週やる / 残す / 中止                                                                          |

## 6. 文言

画面の文言は短い常体または体言止めを基本にし、ユーザーを責める表現を使いません。たとえば、振り返りを書かなかった日は「未記入」ではなく「記録なし」、FBがない日は「まだFBをもらっていません」と表示します。AIの判定には必ず根拠を添え、ユーザーが修正できることを示します。

## 7. アクセシビリティ

本文のコントラスト比は4.5:1以上とし、小さい文字には `--ink-4` より薄い色を使いません。白の半透明の面の上に置く白文字のボタンは、塗りの不透明度を0.85以上にします。すべての操作要素は実際の `button`、`a`、`input` で作り、キーボードフォーカスを藍色のリングで常に見えるようにします。アイコンだけのボタンには `aria-label` を付け、FBの生成状態が変わる領域には `aria-live="polite"` を付けます。
