---
title: "日付ごとの記録のスナップショットと「記録を再生成」"
labels: [type:feat, priority:p2, area:server, status:blocked]
milestone: M4
depends_on: [018, 020]
---

## 背景

architecture.md 4.6。FB の入力を再現できるようにし、記録の不整合に気づいたときのフォールバックとして再生成を用意する。

## やること

- [ ] `daily_records` テーブルとスナップショットの保存（振り返りの保存時、FB依頼時）
- [ ] 再生成のAPIと、再生成前後の差分の返却
- [ ] 日付の詳細に「記録を再生成」ボタンと差分の表示、FB再依頼の選択

## 完了条件

- 再生成で内容が変わった場合に差分が表示される
- FB の入力にスナップショットが使われていることがテストで確認できる

## 関連

FR-R07
