---
title: "送信内容のプレビュー"
labels: [type:feat, priority:p1, area:agent, status:blocked]
milestone: M3
depends_on: [034, 028]
---

## 背景

FR-A12、architecture.md 12.5「送信内容のプレビュー」。issue 030 の決定で、AI に送るデータの確認機能として最初に入れる。

## やること

- [ ] `POST /api/agent-input/preview`（payload、annotations、charCount、payloadHash）
- [ ] `POST /api/jobs` の `payloadHash` の検証と 409（`PREVIEW_STALE`）
- [ ] 振り返り画面の「送信内容を見る」と、詳細ペインでの項目ごとの表示（注記の強調と理由）
- [ ] 設定「依頼の前に毎回確認する」と、有効時にプレビューを経由する流れ
- [ ] DESIGN.md にプレビューの表示ルールを追記する

## 完了条件

- プレビューの内容と、偽アダプタが受け取った入力が一致することを確かめる E2E がある
- プレビューの後に振り返りを変更して依頼すると、再確認を求められる E2E がある

## 関連

FR-A12、NFR-15、issue 030、issue 034
