---
title: "M1：Web アプリの雛形、デザイントークン、Mame コンポーネント"
labels: [type:feat, priority:p0, area:web, status:ready]
milestone: M1
depends_on: []
---

## 背景

UI の実装 issue の土台。Figma（issue 004）が用意できるまでは `docs/design/mockup-source/` を見た目の参照にする。

## やること

- [ ] `apps/web` を Vite ＋ React ＋ TanStack Query で作る
- [ ] DESIGN.md の表から `src/styles/tokens.css` を作る（値は表と完全に一致させる）
- [ ] `docs/design/mockup-source/Mame.dc.html` の形状と表情の表をもとに `Mame` コンポーネントを作る
- [ ] 3ペインのレイアウトの枠（サイドバー、メイン、詳細）と、issue 011 の URL のルーティング
- [ ] Vite の `/api` プロキシ（ADR-0007）
- [ ] Vite の待ち受け先を `MYMIND_HOST`、ファイル監視のポーリングを `MYMIND_WATCH_POLLING` で切り替えられるようにする（ADR-0010）
- [ ] Inter と Noto Sans JP（サブセット）を同梱し、`@font-face` で読み込む（NFR-10、architecture.md 12.1）

## 完了条件

- `pnpm dev` で3ペインの枠とサイドバーの画面移動が動く
- `Mame` の7つの表情を並べた確認用ページがある
- `pnpm design:check` が通る
- ネットワークを切った状態でもフォントが表示される

## 関連

DESIGN.md、ADR-0007、FR-U04
