---
title: "M3：AgentRunner の境界、偽アダプタ、ジョブランナー"
labels: [type:feat, priority:p0, area:agent, status:blocked]
milestone: M3
depends_on: [005, 018]
---

## 背景

architecture.md 7章。実物のエージェント（issue 001）を待たずに、FB の流れ全体を偽アダプタで作れるようにする。

## やること

- [ ] `AgentRunner` インターフェースと、決まった JSON を返す `FakeAgentRunner`（成功・形式違反・タイムアウト・キャンセルを切り替え可能）
- [ ] `agent_jobs` テーブル、同時実行1件のジョブランナー、起動時の中断処理
- [ ] `POST /api/jobs`、ジョブの進捗を `GET /api/events`（SSE、issue 032）で配信、`POST /api/jobs/:id/cancel`
- [ ] `MYMIND_AGENT=fake` で偽アダプタを選ぶ設定

## 完了条件

- 偽アダプタで、依頼→生成中→完了／失敗／キャンセルの結合テストと E2E がある

## 関連

FR-A01、FR-A08、ADR-0003
