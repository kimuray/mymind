---
title: "M1：今日とバックログのAPI"
labels: [type:feat, priority:p0, area:server, status:blocked]
milestone: M1
depends_on: [005, 018, 019]
---

## 背景

issue 005、018、019 に依存。

## やること

- [ ] `GET /api/days/:day`、`POST /api/tasks`、`PATCH /api/tasks/:id`、`POST /api/tasks/:id/transition`、`POST /api/tasks/:id/move`、`GET /api/backlog`

## 完了条件

- 各APIの結合テストがあり、zod の検証エラーが400で返る
- `version` の不一致と `expectedDay` の不一致がそれぞれ 409 で返る（NFR-13、NFR-14）

## 関連

FR-T01、FR-T03、FR-T05
